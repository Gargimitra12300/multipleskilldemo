#!/usr/bin/env bash
# A DEEPER nested resource (two levels under the skill dir: scripts/lib/). Its output proves that
# nested subfolders more than one level deep were preserved during skill placement.
echo "DEEP_NESTED_RAN_9K2X"
